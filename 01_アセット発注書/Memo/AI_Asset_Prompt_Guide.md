# 🎨 AI Asest Prompt Generation Guide

このドキュメントは、AIが本プロジェクト（Novel PWA）のJSONデータを基に、画像生成AI用のアセット発注プロンプトを作成するためのガイドラインです。

## 1. JSON駆動開発 (JSON-Driven Development) の哲学

本プロジェクトでは、「シナリオ」「キャラクターデータ」「アセット管理」を全てJSONで定義し、それを正（Single Source of Truth）として開発を進めます。
AIはこれらのJSONを読み解き、不足している素材を特定し、統一されたフォーマットで発注書を作成する役割を担います。

## 2. アセット発注ワークフロー

AIは以下の手順でアセットの発注（プロンプト作成）を行います。

1.  **シナリオ分析 (`scenario.json`)**
    *   シナリオ内で使用されている `sceneTags` (背景) や `speaker` (キャラクター) を確認します。
    *   イベント演出 (`event`) で必要なCG（一枚絵）がないか確認します。
2.  **ライブラリー確認 (`src/assets`, `AssetPaths.json`)**
    *   必要なアセットが既に存在するか確認します。
    *   `src/assets/AssetPaths.json` や `public/data/gallery.json` を参照します。
3.  **発注書の作成 (`*_order.json`)**
    *   不足しているアセットがある場合、`01_アセット発注書` ディレクトリ内に新しい発注書JSONを作成、または既存のJSONに追記します。
    *   ファイル名の例: `npc_order.json`, `bg_order_chapter1.json`

## 3. アセット発注書 (Order JSON) の構造

発注書は以下のJSON構造・スキーマに従って作成してください。

### 基本構造 (Template)

```json
{
    "//": "🎨 アセット発注書テンプレート",
    "ORDER_INFO": { "ORDER_DATE": "YYYY-MM-DD", "PROJECT": "Novel PWA", "STATUS": "未着手" },
    "CHARACTERS": [ /* キャラクター発注リスト */ ],
    "BACKGROUNDS": [ /* 背景発注リスト */ ],
    "ITEMS": [ /* アイテム発注リスト */ ],
    "TECHNICAL_SPECS": {
        "FORMAT": "PNG (透過あり)",
        "STYLE": "アニメ調イラスト、ファンタジーRPG風"
    }
}
```

### キャラクター定義 (`VISUAL_REFERENCE`)

画像生成プロンプトの元となる、視覚的特徴を詳細に記述します。

```json
"VISUAL_REFERENCE": {
    "AGE": "年齢イメージ (例: 20代後半)",
    "HAIR": "髪型・色 (例: 銀髪、ショートボブ)",
    "BUILD": "体格 (例: 華奢、長身)",
    "OUTFIT": "服装 (例: 魔法使いのローブ、星の装飾)",
    "ACCESSORIES": "装飾品 (例: 水晶の杖)",
    "PERSONALITY_VISUAL": "性格からくる視覚的特徴 (例: 意志の強い瞳)"
}
```

### アセット詳細 (`ASSETS`)

具体的なファイル名とポーズ・表情を指定します。

```json
"ASSETS": {
    "STANDING_ILLUSTRATION": {
        "FILENAME": "chara/name/standing.png",
        "DESCRIPTION": "立ち絵の説明",
        "POSE": "ポーズ指定"
    },
    "FACE_ICONS": [
        { "EXPRESSION": "笑顔", "FILENAME": "chara/name/faces/smile.png" }
    ]
}
```

## 4. プロンプト生成ストラテジー

JSONの `VISUAL_REFERENCE` から画像生成プロンプトを作成する際は、以下のルールに従います。

*   **スタイル指定**: `TECHNICAL_SPECS.STYLE` を必ず文頭または文末に含める（例: "anime style, fantasy RPG art"）。
*   **特徴の列挙**: `VISUAL_REFERENCE` の各項目を英語のカンマ区切りタグに変換する。
    *   `HAIR: 銀髪` -> `silver hair`
    *   `OUTFIT: 魔法使いのローブ` -> `wizard robe, star decoration`
*   **状態の指定**: `ASSETS` 内の `POSE` や `EXPRESSION` を反映する。

### プロンプト生成例

**JSON:**
```json
"VISUAL_REFERENCE": { "HAIR": "金髪ロング", "OUTFIT": "白い騎士鎧" },
"ASSETS": { "POSE": "剣を構える" }
```

**Generative Prompt:**
> anime style, fantasy rpg character, female knight, long blonde hair, white knight armor, holding a sword, battle stance, white background, high quality
